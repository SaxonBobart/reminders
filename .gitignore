# ---------- macOS ----------
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes
.fseventsd
.DocumentRevisions-V100
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# ---------- Xcode ----------
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM
*.dSYM.zip
*.xcodeproj/project.xcworkspace/xcuserdata/

# ---------- Swift Package Manager ----------
.build/
Packages/
.swiftpm/
# Note: Package.resolved IS committed so builds are reproducible.

# ---------- CocoaPods / Carthage (not used today, here as a safety net) ----------
Pods/
Carthage/Build/

# ---------- Editors ----------
.vscode/*
!.vscode/extensions.json
.idea/
*.iml
*.swp
*.swo

# ---------- Env / secrets ----------
.env
.env.*
.env.local

# ---------- SQLite (GRDB) ----------
# The app's local DB lives under Application Support on-device, outside the repo.
# These patterns are a safety net in case a sample/fixture DB ever lands in the tree.
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# ---------- Misc ----------
*.log
.tuist-*
node_modules/
.claude/worktrees/
